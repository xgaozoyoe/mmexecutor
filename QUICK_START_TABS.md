# 快速开始 - Tab 切换功能

## 🎯 效果预览

现在你可以使用 **Tab 按钮**在不同交易所之间快速切换！

```
┌─────────────────────────────────────────┐
│      🤖 Trading Bot Dashboard           │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  [  MEXC  ] [ Gate.io ] [ KuCoin ]      │
│     ↑ 当前选中（紫色高亮）               │
└─────────────────────────────────────────┘

显示选中交易所的数据 ↓
┌─────────────────────────────────────────┐
│  📊 MEXC                                 │
│  账户余额、市场深度、订单等...            │
└─────────────────────────────────────────┘
```

## 🚀 使用方法

### 1. 重新构建前端
```bash
cd frontend
npm run build
```

### 2. 启动服务
```bash
# 回到项目根目录
cd ..

# 启动 report 服务
./target/release/mexc-grid-trader report
```

### 3. 查看效果

**开发模式（推荐）：**
```bash
cd frontend
npm start
```
浏览器会自动打开

**或使用构建版本：**
```bash
npx serve -s frontend/build -p 8080
```
然后访问 `http://localhost:8080`

### 4. 点击 Tab 切换交易所！
- 点击 "MEXC" → 查看 MEXC 数据
- 点击 "Gate.io" → 查看 Gate.io 数据
- 点击 "KuCoin" → 查看 KuCoin 数据

## ✨ 特点

### 视觉设计
- ✅ **激活状态**：紫色渐变高亮
- ✅ **未激活状态**：灰色渐变
- ✅ **悬停效果**：按钮上浮 + 阴影
- ✅ **流畅动画**：0.3 秒过渡效果

### 智能行为
- ✅ **自动隐藏**：只有 1 个交易所时不显示 Tab
- ✅ **默认选择**：自动选中第一个交易所
- ✅ **即时切换**：无需加载，点击即切换
- ✅ **响应式设计**：桌面和手机都完美显示

### 移动端优化
- ✅ 手机上 Tab 自动垂直排列
- ✅ 全宽按钮，易于点击
- ✅ 大号触摸区域

## 📱 不同设备的显示

### 桌面（宽屏）
```
┌────────────────────────────────────────────────┐
│  [ MEXC ]  [ Gate.io ]  [ KuCoin ]             │
│   横向排列，每个按钮等宽                         │
└────────────────────────────────────────────────┘
```

### 手机（窄屏）
```
┌──────────────┐
│    MEXC      │
├──────────────┤
│   Gate.io    │
├──────────────┤
│   KuCoin     │
└──────────────┘
  垂直排列
```

## 💡 使用场景

### 场景 1：多交易所对比
1. 点击 "MEXC" → 看到平均价格 0.015050
2. 点击 "Gate.io" → 看到平均价格 0.015100
3. 对比发现：MEXC 买入价格更好！

### 场景 2：快速检查
1. 点击每个 Tab
2. 快速查看各交易所的：
   - 账户余额
   - 持仓情况
   - PnL 表现
   - 平均交易价格

### 场景 3：单交易所（无 Tab）
如果你的 `config.json` 只配置了 1 个交易所：
- Tab 不会显示
- 直接显示交易所数据
- 界面更简洁

## 🎨 自定义

### 修改 Tab 颜色
编辑 `frontend/src/App.css`：

```css
.tab.active {
  /* 激活状态的颜色 */
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  /* 改成你喜欢的颜色，例如绿色： */
  /* background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); */
}
```

### 修改 Tab 大小
```css
.tab {
  padding: 15px 25px;    /* 调整内边距 */
  font-size: 1.1em;      /* 调整字体大小 */
}
```

## ❓ 常见问题

### Q: 为什么看不到 Tab？
**A:** 你的配置文件只有 1 个交易所。Tab 只在 2+ 交易所时显示。

### Q: 切换 Tab 时数据会重新加载吗？
**A:** 不会！所有数据已经加载，切换是即时的。

### Q: 可以记住我上次选择的 Tab 吗？
**A:** 目前还不支持，刷新页面会回到第一个 Tab。未来版本可能会添加此功能。

### Q: 移动端显示不正常？
**A:** 清除浏览器缓存并硬刷新（Ctrl+Shift+R）。

## 🔍 检查是否生效

### 方法 1：查看构建文件
```bash
grep "tabs-container" frontend/build/static/css/main.*.css
```
如果有输出，说明 CSS 已包含。

### 方法 2：浏览器开发者工具
1. 打开浏览器
2. 按 F12 打开开发者工具
3. 在 "Elements" 标签查找 `<div class="tabs-container">`
4. 如果存在，说明功能已生效

### 方法 3：直接测试
1. 打开浏览器
2. 如果有 2+ 交易所，应该能看到 Tab 按钮
3. 点击不同的 Tab，内容会切换

## 📋 完整步骤

```bash
# 1. 进入前端目录
cd /Users/xingao/mmexecutor/frontend

# 2. 重新构建
npm run build

# 3. 回到根目录
cd ..

# 4. 启动 report 服务
./target/release/mexc-grid-trader report

# 5. 新终端启动前端开发服务器
cd frontend
npm start

# 6. 浏览器会自动打开，查看 Tab 效果！
```

## 🎉 享受新功能

现在你可以：
- ✅ 快速切换查看不同交易所
- ✅ 对比各交易所表现
- ✅ 清爽的单页面显示
- ✅ 无需滚动浏览长页面

祝使用愉快！🚀

---

**更多信息：**
- 详细文档：[TABS_FEATURE.md](TABS_FEATURE.md)
- 完整指南：[REPORT_DASHBOARD.md](REPORT_DASHBOARD.md)
